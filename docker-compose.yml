services:
  backend:
    build: ./backend
    image: backend-app
    ports:
      - "3000:3000"
    env_file:
      - ./backend/config.env  

  frontend:
    build: ./frontend
    image: frontend-app
    ports:
      - "3001:3000"  
    env_file:
      - ./frontend/.env    